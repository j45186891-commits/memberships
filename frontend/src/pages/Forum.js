import React from 'react';
import { Box, Typography } from '@mui/material';

export default function Forum() {
  return (
    <Box>
      <Typography variant="h4">Forum</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>Community discussions.</Typography>
    </Box>
  );
}
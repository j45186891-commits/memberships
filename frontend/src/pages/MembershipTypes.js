import React from 'react';
import { Box, Typography } from '@mui/material';

export default function MembershipTypes() {
  return (
    <Box>
      <Typography variant="h4">Membership Types</Typography>
      <Typography variant="body1" sx={{ mt: 2 }}>Manage membership types and pricing.</Typography>
    </Box>
  );
}